{
  "name": "threshold_73",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "backend:dev": "python -m uvicorn backend.app.main:app --reload --app-dir .",
    "backend:start": "python -m uvicorn backend.app.main:app --app-dir ."
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  }
}
